/**
 * Presets de motion — preferir estes em novas telas para consistência.
 * @see `easings.ts` (springs, tweens, durações) e `variants.ts` (stagger, page shell).
 */
export * from './easings';
export * from './variants';
